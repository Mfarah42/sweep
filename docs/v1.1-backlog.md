# v1.1 backlog (explicit non-goals for v1 — spec §14)

- **Auto-detect parking**: CLVisit / CarPlay-Bluetooth disconnect heuristics
  (requires Always location posture review).
- ~~**Dark mode**~~ Shipped 2026-07-09 by user request: adaptive tokens
  ("paper after sunset" — warm charcoal, cream ink), Settings picker
  System/Light/Dark, sign preview intentionally stays physical-white.
- **Anonymous correction telemetry**: opt-in flag POST when a user submits a
  "sign says otherwise" correction (v1 keeps corrections on-device only).
- **Longest-spot map visualization**: v1 ships the ranked list only.
- **Android.**
- **Additional cities**: each new city = `pipeline/adapters/{city}.py` +
  `pipeline/landmarks/{city}.yaml` + holiday entries + `City` enum case.
- **Landmark copy review**: port the prototype's eight editorial entries
  verbatim when `sweep-app.jsx` is available; current entries are authored
  stand-ins in the same voice.
